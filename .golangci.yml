version: "2"

linters:
  default: none
  enable:
    - gosimple
    - govet
    - ineffassign
    - misspell
    - unconvert
    - unused
  exclusions:
    paths:
      - testdata

formatters:
  enable:
    - gofmt
  exclusions:
    paths:
      - testdata
