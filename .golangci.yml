version: "2"

linters:
  default: none
  enable:
    - staticcheck
    - govet
    - ineffassign
    - misspell
    - unconvert
    - unused
  exclusions:
    paths:
      - testdata

formatters:
  enable:
    - gofmt
  exclusions:
    paths:
      - testdata
